require "spec_helper"

RSpec.describe Whaletamer do
  it "has a version number" do
    expect(Whaletamer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
